import 'package:flutter/material.dart';

abstract final class ElkStayColors {
  static const pine = Color(0xFF12433A);
  static const pineDeep = Color(0xFF0C322B);
  static const pineSoft = Color(0xFFE6EFEA);
  static const honey = Color(0xFFF0A93A);
  static const honeyDeep = Color(0xFFD88E1E);
  static const paper = Color(0xFFFAF7F1);
  static const ink = Color(0xFF18211E);
  static const muted = Color(0xFF6C7872);
  static const line = Color(0xFFE7E2D8);
  static const canvas = Color(0xFF0E2B25);

  static const pgGradient = LinearGradient(
    colors: [Color(0xFF1A5547), Color(0xFF0E3A30)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );
  static const mensGradient = LinearGradient(
    colors: [Color(0xFF2C6E5C), Color(0xFF184C40)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );
  static const womensGradient = LinearGradient(
    colors: [Color(0xFFC97D2A), Color(0xFFA85F16)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );
  static const homestayGradient = LinearGradient(
    colors: [Color(0xFF3A6B5E), Color(0xFF244C42)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );
  static const heroGradient = LinearGradient(
    colors: [Color(0xFF1C5044), Color(0xFF3A7261)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );
  static const profileGradient = LinearGradient(
    colors: [Color(0xFF12433A), Color(0xFF0C322B)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );
}
